---
- name: Add Python Deadsnakes PPA
  apt_repository: repo="ppa:deadsnakes" state="present"

- name: Update apt cache
  apt:  update_cache=yes

- name: Install package
  apt: name="python{{ python_version }}" state="present"
